// Relocated to src/features/auth/hooks/useAuth.ts — this file is kept only as a
// transitional re-export and must not be imported directly.
export { AuthProvider, useAuth } from '../features/auth/hooks/useAuth';
